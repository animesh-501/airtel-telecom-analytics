# Airtel Telecom Analytics Pipeline

> End-to-end analytics pipeline for a telecom operator — synthetic data generation, cleaning, EDA, churn prediction & revenue forecasting.  
> **Animesh Choubey** · [linkedin.com/in/choubey-animesh](https://linkedin.com/in/choubey-animesh) · [animesh-501.github.io](https://animesh-501.github.io)

---

## Business Problem

Telecom operators lose 2–3% of their subscriber base monthly to churn. At scale, that's millions in lost recurring revenue. The problem isn't identifying that churn happens — it's knowing *which customers are about to leave, why, and what it costs to retain them.*

This pipeline answers three operational questions:
1. **Who is at risk?** — Score every customer by churn probability
2. **Why are they leaving?** — Identify the dominant signals (billing arrears, support escalations, network issues)
3. **What's the revenue impact?** — Forecast 6-month revenue under current churn trajectory

---

## Key Results

| Metric | Value |
|--------|-------|
| Customers modeled | 1,000 seed + 2,000 SDV-synthetic |
| Billing records | 5,000 seed + 8,000 SDV-synthetic |
| Churn rate in dataset | ~40% |
| Best model AUC (PyCaret AutoML) | ~0.85+ |
| Prophet forecast horizon | 6 months |
| Risk tiers | Low / Medium / High / Critical |
| At-risk list exported | Top 50 customers for retention intervention |

---

## Pipeline

```
01_data_generation.ipynb   →   Faker + SDV synthesis + YData Profiling
02_data_cleaning.ipynb     →   Deduplication, type coercion, SQL validation
03_eda.ipynb               →   Churn patterns, billing analysis, CSAT trends
04_forecasting.ipynb       →   PyCaret AutoML + Prophet + Risk scoring
```

Each notebook is self-contained and runs independently (after Phase 1 outputs exist).

---

## Tech Stack

| Layer | Tools |
|-------|-------|
| Data Generation | `Faker` (Indian locale) · `SDV` GaussianCopulaSynthesizer |
| Profiling | `YData Profiling` HTML reports |
| Cleaning | `pandas` · `SQLite` · `SQLAlchemy` |
| EDA | `matplotlib` · `seaborn` |
| ML | `PyCaret` AutoML (classification) |
| Forecasting | `Prophet` (time series) |
| Config | `config.py` · `utils.py` — shared across all notebooks |

---

## Repository Structure

```
airtel-telecom-analytics/
│
├── config.py                    ← All constants: sizes, dirs, weights, Prophet params
├── utils.py                     ← Shared functions: logging, risk scoring, chart helpers
├── requirements.txt
│
├── 01_data_generation.ipynb     ← Faker + SDV + YData Profiling
├── 02_data_cleaning.ipynb       ← Cleaning + SQL validation + deduplication
├── 03_eda.ipynb                 ← EDA: churn patterns, billing, CSAT, correlations
├── 04_forecasting.ipynb         ← PyCaret AutoML + Prophet + risk tier export
│
├── airtel_raw_data/             ← Generated by Phase 1
├── airtel_clean_data/           ← Generated by Phase 2
├── airtel_eda_outputs/          ← Generated by Phase 3
└── airtel_forecast_outputs/     ← Generated by Phase 4
```

---

## Setup & Run

```bash
git clone https://github.com/animesh-501/airtel-telecom-analytics
cd airtel-telecom-analytics
pip install -r requirements.txt
jupyter lab
```

Run notebooks in order: `01 → 02 → 03 → 04`

---

## Key Engineering Decisions

**`config.py` as single source of truth** — all constants (dataset sizes, noise rates, Prophet params, risk weights) live in one file. No magic numbers scattered across notebooks.

**`utils.py` for shared logic** — `force_numeric()`, `snapshot()`, `save_fig()`, `compute_risk_score()` defined once and imported everywhere. No copy-paste across notebooks.

**O(1) billing lookup** — original approach filtered a 5,000-row DataFrame inside a loop (O(n²)). Replaced with a dict lookup built once before the loop. Significant speedup at scale.

**Prophet changepoint tuning** — `changepoint_prior_scale` reduced from `0.3` → `0.05`. With only ~12 months of training data, the default value overfit to noise. Lower scale produces smoother, more reliable forecasts.

**Local RandomState in noise injection** — `inject_noise()` uses a local `np.random.RandomState` rather than the global seed, so noisy output is reproducible without polluting other random calls in the notebook.

**Risk weights documented in one place** — `RISK_WEIGHTS` dict in `utils.py` explains what each factor measures and why it's weighted that way. Tunable without touching scoring logic.

---

*Part of Animesh Choubey's analytics portfolio — [animesh-501.github.io](https://animesh-501.github.io)*
